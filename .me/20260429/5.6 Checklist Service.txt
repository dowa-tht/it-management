function Service_GetChecklist(IncId: string): array of record
    Id     : string;
    Type   : (Daily, Weekly, Monthly);
    Status : (Pending, Done, Skipped);
    Label  : string;
  end;
begin
  // SELECT จากตาราง checklist WHERE incident_id = IncId
end;

function Service_SaveChecklistItem(ChkId: string; NewStatus: (Pending, Done, Skipped)): boolean;
begin
  // UPDATE status
end;
